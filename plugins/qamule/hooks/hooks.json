{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "if test -f qamule.lock; then uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/inject-session-context.py\"; fi",
        "cwd": "."
      }
    ]
  }
}
